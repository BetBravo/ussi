{% extends 'base.html.twig' %}

{% block body %}
    <h1>Signosvitalessuministrados list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Valor</th>
                <th>Nombre</th>
                <th>Fecha</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for signosVitalesSuministrado in signosVitalesSuministrados %}
            <tr>
                <td><a href="{{ path('signosvitalessuministrados_show', { 'id': signosVitalesSuministrado.id }) }}">{{ signosVitalesSuministrado.id }}</a></td>
                <td>{{ signosVitalesSuministrado.valor }}</td>
                <td>{{ signosVitalesSuministrado.nombre }}</td>
                <td>{% if signosVitalesSuministrado.fecha %}{{ signosVitalesSuministrado.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('signosvitalessuministrados_show', { 'id': signosVitalesSuministrado.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('signosvitalessuministrados_edit', { 'id': signosVitalesSuministrado.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('signosvitalessuministrados_new') }}">Create a new signosVitalesSuministrado</a>
        </li>
    </ul>
{% endblock %}
