{% extends 'base.html.twig' %}

{% block body %}
    <h1>Insumosuministrado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insumoSuministrado.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ insumoSuministrado.cantidad }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{% if insumoSuministrado.fecha %}{{ insumoSuministrado.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('insumosuministrado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('insumosuministrado_edit', { 'id': insumoSuministrado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
